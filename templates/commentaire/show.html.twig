{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>NoteMoto</th>
                <td>{{ commentaire.noteMoto }}</td>
            </tr>
            <tr>
                <th>TexteMoto</th>
                <td>{{ commentaire.texteMoto }}</td>
            </tr>
            <tr>
                <th>NoteProprio</th>
                <td>{{ commentaire.noteProprio }}</td>
            </tr>
            <tr>
                <th>TexteProprio</th>
                <td>{{ commentaire.texteProprio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
